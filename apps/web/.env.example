# Destino del proxy del servidor Next.js hacia NestJS.
API_URL=http://localhost:3000
# El navegador usa el proxy del mismo origen para evitar depender de CORS.
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_AUTH_MOCK=false
